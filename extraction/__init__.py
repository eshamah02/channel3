"""Site-agnostic HTML -> Product extraction.

Every module here works from web standards (schema.org JSON-LD, microdata,
OpenGraph, HTML semantics) or from generic structural properties. No module
branches on a domain, and no selector is tuned to one site's markup.
"""
